Transverse folder python script goes through a selected folder to list the contents of the root folders and the contents of its subfolders in a chronological and alphabetical order in an excel output. The detials included are file location, filename, date created, file type and filesize. The output CSV has the same name as the root folder and saved in it. 

The pyton script can be run in Terminal or Visual Studio Code or anywhere else possible. 
